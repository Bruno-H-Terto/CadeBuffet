<p><%= link_to 'Meu Buffet', buffet_path(@buffet)%> > Cadastrar novo evento</p>

<% if @event.errors.any? %>
  <p>Reveja os campos abaixo: </p>
  <ul>
  <% @event.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>
<% if @price.errors.any? %>
  <p>Reveja os campos abaixo: </p>
  <ul>
  <% @price.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>


<main>
  <h2>Cadastrar novo evento</h2>
  <%= render 'form' %>
</main>