<% if @historic.present? %>
  <% @historic.each do |order| %>
    <dl>
      <dt><%= Order.human_attribute_name :code%> <%= link_to "#{order.code}", order_view_path(order) %></dt>
      <dd><%= Buffet.human_attribute_name :estimated_date%>: <%= I18n.l order.estimated_date %></dd>
      <dd><%= Order.human_attribute_name :status%>: <%= Buffet.human_attribute_name order.status %></dd>
    </dl>
  <% end %>
<% else %>
  <p>Não existem pedidos registrados %>
<% end %>